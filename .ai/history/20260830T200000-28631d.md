# PERFORMANCE 4Deck対応

- Change ID: `20260830T200000-28631d`
- 作成日時: 2026-08-30 20:00 JST
- Git commit: `28631d4de001331d8aceaa96bf57f778cd9c2ac6`

## 目的

rekordbox Deck 1/2中心だったショートカット制御を、既存の4 Group構造を維持したままPERFORMANCE Deck 1〜4へ拡張し、Deck別commandIdのコピーを増やさない構造にする。

## 実施内容

- `OverCUEGroupMapping`へ`rekordboxDeck`を追加し、config versionを8へ更新した。
- Deck依存Actionを共通suffixと対象Deckから解決するよう`RekordboxActionAdapter`を変更した。
- Generic `rekordbox:<commandId>`を変換対象外として維持した。
- 旧既定Group 2のDeck 2直書きcommandIdを標準Actionへ移行するmigrationを追加した。
- Shortcut GUIへDeck 3/4カテゴリとGroup別target Deck Pickerを追加した。
- BridgeがGroupごとのmode、Deck、波形位置を切り替えるよう統合した。
- `Scripts/verify-macos.sh`を追加し、ローカル検証を一括実行できるようにした。
- 仕様、README、日本語／英語／簡体字中国語ドキュメントを更新した。

### 対象ファイル

```text
README.md
Scripts/build-app.sh
Scripts/verify-macos.sh
Sources/OverCUEApp/
Sources/OverCUEBridge/main.swift
Sources/OverCUEChecks/main.swift
Sources/OverCUECore/
docs/
specs/current-spec.md
```

## 検証

- `swift build`: 成功。
- `swift build -c release`: 成功。
- `swift run overcue-checks`: 241件すべて成功。
- `./Scripts/verify-macos.sh`: 成功。
- `dist/OverCUE.app`を生成。
- `OverCUE`と`overcue-cli`の両方でarm64 + x86_64を確認。
- ad-hoc codesignと`codesign --verify --deep --strict`に成功。
- `open dist/OverCUE.app`後、アプリ本体と内蔵CLIのプロセス起動を確認した。

## 残課題

- 選択中`Performance 1 (Preset)`にはDeck 4行がないため、Deck 4=`33xx`をXML実データで直接確認できていない。
- ACK05実機、アクセシビリティ経由のrekordbox操作、Group別波形ドラッグは未検証。
